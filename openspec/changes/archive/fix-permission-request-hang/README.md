# Fix: Permission Request Hang

## Обзор

Исправление бага при котором второй permission request не отображается после одобрения первого, что приводит к зависанию сессии.

## Статус

**Статус**: 📝 Задокументировано (готово к реализации)  
**Приоритет**: 🔴 Высокий  
**Оценка**: 8 часов

## Проблема

При последовательных запросах с permission:
1. Первый permission request отображается и обрабатывается корректно
2. После одобрения первого запроса, второй permission request ставится в очередь
3. Второй запрос НЕ обрабатывается
4. Сессия зависает на 5 минут до timeout

## Документация

- [proposal.md](./proposal.md) - Описание проблемы и влияние
- [design.md](./design.md) - Технический дизайн решения
- [tasks.md](./tasks.md) - План реализации
- [specs/](./specs/) - Формальные спецификации

## Быстрый старт

### Воспроизведение бага

```bash
# 1. Запустить клиент
codelab

# 2. Отправить первый prompt
> прочти README.md

# 3. Одобрить permission на fs/read_text_file

# 4. Отправить второй prompt
> выполни команду ls -ahl .

# 5. Наблюдать: permission request НЕ отображается
# 6. Ожидать 5 минут до timeout
```

### Проверка исправления

После применения исправления:
```bash
# Повторить шаги 1-4
# Второй permission request ДОЛЖЕН отобразиться сразу
```

## Структура задачи

```
fix-permission-request-hang/
├── README.md              # Этот файл
├── proposal.md            # Описание проблемы
├── design.md              # Технический дизайн
├── tasks.md               # План реализации
└── specs/
    └── permission-processing.md  # Спецификация
```

## Связанные файлы

### Код

- `src/codelab/client/infrastructure/services/acp_transport_service.py`
  - Метод: `_wait_for_response_with_events` (строки 613-692)
  - Метод: `_handle_permission_task` (строки 694-730)

### Тесты

- `tests/client/test_acp_transport_service.py`
- `tests/client/test_permission_handler_direct_lifecycle.py`

### Логи

- `~/.codelab/logs/codelab-34685.log` - Логи с воспроизведением бага

## Ключевые моменты

### Корневая причина

В `_wait_for_response_with_events` после обработки permission request, новый `permission_task` создаётся в конце цикла обработки. Это создаёт race condition: если следующий permission request приходит в очередь до того как новый task начнёт ожидать, сообщение может быть потеряно.

### Решение

Создавать новый `permission_task` **сразу** после обработки предыдущего, не в конце цикла. Добавить логирование для отладки.

### Влияние

- **Критичность**: Высокая - блокирует использование клиента
- **Частота**: Всегда воспроизводится при последовательных permission requests
- **Workaround**: Перезапуск клиента (неудобно)

## Метрики успеха

1. Второй permission request отображается сразу после одобрения первого
2. Нет зависаний сессии при последовательных permission requests
3. Все существующие тесты проходят
4. Новые тесты покрывают сценарий множественных permission requests

## Следующие шаги

1. Review документации (proposal, design, specs)
2. Утвердить план реализации
3. Назначить исполнителя
4. Реализовать исправление согласно tasks.md
5. Протестировать согласно тест-плану
6. Создать PR и пройти code review
7. Merge в main

## Контакты

- **Автор**: OpenCode Agent
- **Дата создания**: 2026-06-18
- **Последнее обновление**: 2026-06-18

## Связанные задачи

- [client-decomposition](../client-decomposition/) - Декомпозиция клиента (контекст)
