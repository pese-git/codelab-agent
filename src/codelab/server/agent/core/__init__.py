"""Чистое ядро агента (hexagon core).

Бизнес-логика turn-а без ACP-специфики: ExecutionEngine, стратегии вызова LLM,
построение системного промпта и истории, санитизация и фильтрация инструментов.

Зависит только от портов (`agent.contracts.ports`) и доменных VO — не от `protocol`.
"""
