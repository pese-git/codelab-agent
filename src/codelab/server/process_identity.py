"""Токен процесса: отличить «своё» состояние от оставшегося от прошлого запуска.

Часть состояния сессии осмысленна только внутри процесса, который её породил.
Пример — реестр терминалов: alias'ы персистятся вместе с сессией, а сами терминалы
живут на стороне клиента и рестарт сервера не переживают. После перезапуска модель
обращается к мёртвому дескриптору и получает `RPC Error -32603` (P2-44).

Токен позволяет отличить два случая, которые иначе выглядят одинаково: сессия
загружена тем же процессом (состояние ещё живо) или другим (состояние мертво).
Хранить pid недостаточно — операционная система переиспользует их.
"""

from __future__ import annotations

from uuid import uuid4

# Уникален на запуск процесса. Не персистится сам по себе — попадает на диск только
# как отметка владельца в тех структурах, которым это нужно.
PROCESS_TOKEN: str = uuid4().hex
